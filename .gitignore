# dependencies
node_modules/

# build output
dist/
build/
*.tsbuildinfo

# turbo / vite caches
.turbo/
.vite/
coverage/

# drizzle generated artifacts (committed migrations under packages/db/migrations are NOT ignored)
packages/db/drizzle/
*.local

# env — real secrets never committed. .env.example IS committed.
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store
